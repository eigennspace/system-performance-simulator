{
  "name": "@sim/simulation-engine",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sim/shared-types": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
